<Window x:Class="LaboratoryApp.src.Modules.Assignment.Common.Views.InsertExerciseSetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LaboratoryApp.src.Modules.Assignment.Common.Views"
        xmlns:domain="clr-namespace:LaboratoryApp.src.Core.Domains"
        xmlns:helper="clr-namespace:LaboratoryApp.src.Core.Helpers"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Height="255"
        Width="400"
        Title="Nhập bộ mới">
    
    <StackPanel Margin="10">
        <TextBlock Text="Thông tin bộ"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0,0,0,5"/>
        
        <TextBox VerticalAlignment="Top"
                 materialDesign:HintAssist.Hint="Mã bộ bài tập"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 Margin="0,0,0,20">
            <TextBox.Text>
                <Binding Path="Code"
                         UpdateSourceTrigger="PropertyChanged">
                    <Binding.ValidationRules>
                        <domain:NotEmptyValidationRule ValidatesOnTargetUpdated="True"/>
                    </Binding.ValidationRules>
                </Binding>
            </TextBox.Text>
        </TextBox>

        <PasswordBox VerticalContentAlignment="Top"
                     materialDesign:HintAssist.Hint="Mật khẩu (Nếu có)"
                     materialDesign:TextFieldAssist.HasClearButton="True"
                     helper:PasswordBoxHelper.Attach="True"
                     helper:PasswordBoxHelper.Password="{Binding Password, Mode=TwoWay}"
                     Style="{StaticResource MaterialDesignOutlinedRevealPasswordBox}"
                     Margin="0,0,0,10"/>

        <Button Width="150"
                Content="Done"
                Background="SpringGreen"
                Foreground="Black"
                BorderThickness="0"
                Style="{StaticResource MaterialDesignRaisedSecondaryButton}"
                materialDesign:ButtonAssist.CornerRadius="10"
                Command="{Binding SaveCommand}"
                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
    </StackPanel>
</Window>
