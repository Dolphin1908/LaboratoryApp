<Page x:Class="LaboratoryApp.src.Modules.Assignment.Common.Views.AssignmentMainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:LaboratoryApp.src.Modules.Assignment.Common.Views"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:wpfui="http://schemas.lepo.co/wpfui/2022/xaml"
      TextElement.Foreground="{DynamicResource MaterialDesignBody}"
      TextElement.FontWeight="Regular"
      TextElement.FontSize="13"
      TextOptions.TextFormattingMode="Ideal"
      TextOptions.TextRenderingMode="Auto"
      Background="{DynamicResource MaterialDesignPaper}"
      FontFamily="{DynamicResource MaterialDesignFont}"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Quản lý bài học">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" 
                    Margin="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0"
                        BorderThickness="1"
                        BorderBrush="Black"
                        CornerRadius="10"
                        Margin="5">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <materialDesign:PackIcon Kind="Magnify" 
                                                 Grid.Column="0"
                                                 Height="20"
                                                 Width="20"
                                                 Margin="5,0"
                                                 VerticalAlignment="Center"/>

                        <Grid Grid.Column="1">
                            <TextBox Name="SearchBox"
                                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                     VerticalAlignment="Center"
                                     BorderThickness="0"
                                     Background="Transparent"
                                     Foreground="Black"
                                     CaretBrush="Black"
                                     Style="{x:Null}"
                                     BorderBrush="Transparent"
                                     Margin="0">
                            </TextBox>

                            <Popup Name="SearchPopup"
                                   PlacementTarget="{Binding ElementName=SearchBox}"
                                   Placement="Bottom"
                                   StaysOpen="False"
                                   IsOpen="{Binding ., Mode=OneWay}"
                                   Margin="0"
                                   VerticalOffset="5"
                                   AllowsTransparency="True"
                                   PopupAnimation="Fade"
                                   Width="{Binding ActualWidth, ElementName=SearchBox}">

                                <ScrollViewer VerticalScrollBarVisibility="Hidden"
                                              Margin="0"
                                              MaxHeight="400">
                                    <ItemsControl ItemsSource="{Binding }">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Button Background="LightGray"
                                                        Cursor="Hand"
                                                        BorderThickness="0"
                                                        Padding="5,0"
                                                        Margin="0"
                                                        HorizontalAlignment="Stretch"
                                                        HorizontalContentAlignment="Left"
                                                        Height="60"
                                                        Command="{Binding }"
                                                        CommandParameter="{Binding .}">
                                                    <!---->
                                                </Button>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                            </Popup>
                        </Grid>
                    </Grid>
                </Border>

                <Button Grid.Column="1"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        BorderThickness="0"
                        Background="Cyan"
                        Margin="5"
                        Height="Auto"
                        Width="Auto"
                        materialDesign:ButtonAssist.CornerRadius="10"
                        Command="{Binding }">
                    <Grid VerticalAlignment="Center"
                          HorizontalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <materialDesign:PackIcon Kind="Add"
                                                 Grid.Column="0"
                                                 Width="20"
                                                 Height="20"
                                                 VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1"
                                   Text="Nhập bộ"
                                   Margin="5,0,0,0"
                                   VerticalAlignment="Center"/>
                    </Grid>
                </Button>
                
                <Button Grid.Column="2"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        BorderThickness="0"
                        Background="SpringGreen"
                        Margin="5"
                        Height="Auto"
                        Width="Auto"
                        materialDesign:ButtonAssist.CornerRadius="10"
                        Visibility="{Binding IsTeacher, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Command="{Binding AddNewSetCommand}">
                    <Grid VerticalAlignment="Center"
                          HorizontalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <materialDesign:PackIcon Kind="Add"
                                                 Grid.Column="0"
                                                 Width="20"
                                                 Height="20"
                                                 VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1"
                                   Text="Thêm bộ mới"
                                   Margin="5,0,0,0"
                                   VerticalAlignment="Center"/>
                    </Grid>
                </Button>
            </Grid>

            <Separator Width="200"
                       Height="1"
                       Background="Gray"
                       BorderBrush="Gray"/>
        </StackPanel>

        <ItemsControl Grid.Row="1"
                      ItemsSource="{Binding ExerciseSets}"
                      Margin="5,0"
                      VerticalAlignment="Top">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="3"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Background="Transparent"
                            BorderBrush="Black"
                            Margin="5"
                            Command="{Binding DataContext.OpenExerciseSetCommand, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"
                            CommandParameter="{Binding .}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Title}"/>
                            <TextBlock Text="-"
                                       Margin="5,0"/>
                            <TextBlock Text="{Binding OwnerInfo.Username}"
                                       Foreground="MediumPurple"/>
                        </StackPanel>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Page>
