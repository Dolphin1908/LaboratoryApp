<Window
    x:Class="LaboratoryApp.src.Modules.Tools.English.DiaryFunction.Views.DiaryDetailWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helper="clr-namespace:LaboratoryApp.src.Core.Helpers"
    xmlns:local="clr-namespace:LaboratoryApp.src.Modules.Tools.English.DiaryFunction.Views"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:wpfui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="{Binding DiaryContent.Title}"
    Width="800"
    Height="450"
    MinWidth="800"
    MinHeight="450"
    Background="{DynamicResource MaterialDesignPaper}"
    FontFamily="{DynamicResource MaterialDesignFont}"
    TextElement.FontSize="13"
    TextElement.FontWeight="Regular"
    TextElement.Foreground="{DynamicResource MaterialDesignBody}"
    TextOptions.TextFormattingMode="Ideal"
    TextOptions.TextRenderingMode="Auto"
    mc:Ignorable="d">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock
                    Margin="0,0,0,5"
                    FontSize="32"
                    FontWeight="Bold"
                    Text="{Binding DiaryContent.Title}" />
                <TextBlock
                    Margin="0,0,0,5"
                    FontSize="14"
                    FontStyle="Italic"
                    Text="{Binding Author.Username}" />
                <TextBlock
                    FontSize="12"
                    FontStyle="Italic"
                    Text="{Binding DiaryContent.CreatedAt}" />
            </StackPanel>

            <StackPanel Grid.Column="1">
                <Button
                    Width="80"
                    Height="32"
                    Margin="0,0,0,5"
                    materialDesign:ButtonAssist.CornerRadius="10"
                    Background="Cyan"
                    BorderThickness="0"
                    Command="{Binding EditCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Visibility="{Binding IsAuthor, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <materialDesign:PackIcon
                            Grid.Column="0"
                            Width="16"
                            Height="16"
                            Kind="Edit" />
                        <TextBlock
                            Grid.Column="1"
                            Margin="5,0,0,0"
                            VerticalAlignment="Center"
                            Text="Sửa" />
                    </Grid>
                </Button>

                <Button
                    Width="80"
                    Height="32"
                    Margin="0,5,0,0"
                    materialDesign:ButtonAssist.CornerRadius="10"
                    Background="Red"
                    BorderThickness="0"
                    Command="{Binding DeleteCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Visibility="{Binding IsAuthor, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <materialDesign:PackIcon
                            Grid.Column="0"
                            Width="16"
                            Height="16"
                            Kind="Delete" />
                        <TextBlock
                            Grid.Column="1"
                            Margin="5,0,0,0"
                            VerticalAlignment="Center"
                            Text="Xóa" />
                    </Grid>
                </Button>
            </StackPanel>

        </Grid>

        <Separator
            Grid.Row="1"
            Margin="0,5"
            HorizontalAlignment="Stretch"
            BorderBrush="Black" />

        <RichTextBox
            Grid.Row="2"
            VerticalContentAlignment="Top"
            helper:RichTextBoxBindingHelper.DocumentXaml="{Binding XamlDocument}"
            BorderThickness="0"
            Focusable="False" />
    </Grid>
</Window>
