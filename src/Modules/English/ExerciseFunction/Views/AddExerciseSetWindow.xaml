<Window x:Class="LaboratoryApp.src.Modules.English.ExerciseFunction.Views.AddExerciseSetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LaboratoryApp.src.Modules.English.ExerciseFunction.Views"
        xmlns:domain="clr-namespace:LaboratoryApp.src.Core.Domains"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        mc:Ignorable="d"
        Height="600" MinHeight="600" 
        Width="400" MinWidth="400"
        Title="Thêm bộ bài tập mới">

    <ScrollViewer Grid.Row="1"
                  VerticalScrollBarVisibility="Hidden">
        <StackPanel Margin="10">
            <!-- Thông tin chung -->
            <TextBlock Text="Thông tin chung"
                       FontSize="20"
                       FontWeight="Bold"
                       Margin="0,0,0,5"/>
            <TextBox VerticalAlignment="Top"
                     materialDesign:HintAssist.Hint="Tên bộ bài tập"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Margin="0,0,0,20">
                <TextBox.Text>
                    <Binding Path="Word"
                             UpdateSourceTrigger="PropertyChanged">
                        <Binding.ValidationRules>
                            <domain:NotEmptyValidationRule ValidatesOnTargetUpdated="True"/>
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>

            <TextBox VerticalAlignment="Top"
                     materialDesign:HintAssist.Hint="Mô tả"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Margin="0,0,0,20"
                     Text="{Binding Pos, UpdateSourceTrigger=PropertyChanged}"/>

            <!-- Cài đặt -->
            <TextBlock Text="Cài đặt"
                       FontSize="20"
                       FontWeight="Bold"
                       Margin="0,0,0,5"/>

            <TextBox VerticalAlignment="Top"
                     materialDesign:HintAssist.Hint="Mã bộ"
                     materialDesign:HintAssist.HelperText="Tùy chọn"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Margin="0,0,0,20"
                     Text="{Binding ., UpdateSourceTrigger=PropertyChanged}"/>

            <PasswordBox VerticalContentAlignment="Top"
                         materialDesign:HintAssist.HelperText="Tùy chọn"
                         materialDesign:HintAssist.Hint="Mật khẩu"
                         materialDesign:TextFieldAssist.HasClearButton="True"
                         Style="{StaticResource MaterialDesignOutlinedRevealPasswordBox}"
                         Margin="0,0,0,20"/>

            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <ComboBox Grid.Column="0"
                          Margin="0,0,5,0"
                          materialDesign:HintAssist.Hint="Loại bộ"
                          Style="{StaticResource MaterialDesignOutlinedComboBox}"
                          ItemsSource="{Binding DataContext.ExerciseTypeOptions, RelativeSource={RelativeSource AncestorType=Window}}"
                          SelectedValuePath="Value"
                          SelectedValue="{Binding SelectedExerciseSetType, UpdateSourceTrigger=PropertyChanged}"/>

                <ComboBox Grid.Column="1"
                          Margin="5,0,0,0"
                          materialDesign:HintAssist.Hint="Độ khó"
                          Style="{StaticResource MaterialDesignOutlinedComboBox}"
                          ItemsSource="{Binding DataContext.DifficultyLevelOptions, RelativeSource={RelativeSource AncestorType=Window}}"
                          SelectedValuePath="Value"
                          SelectedValue="{Binding SelectedDifficultyLevel, UpdateSourceTrigger=PropertyChanged}"/>
            </Grid>
            
            <StackPanel Orientation="Horizontal"
                        Margin="0,0,0,20">
                <CheckBox Content="Công khai"
                          Margin="0,0,20,0"/>
                <CheckBox Content="Yêu cầu đăng nhập"/>
            </StackPanel>

            <!-- Nút -->

            <Button Grid.Column="0"
                    Width="150"
                    Content="Done"
                    Background="SpringGreen"
                    Foreground="Black"
                    BorderThickness="0"
                    Style="{StaticResource MaterialDesignRaisedSecondaryButton}"
                    materialDesign:ButtonAssist.CornerRadius="10"
                    Command="{Binding SaveCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
        </StackPanel>
    </ScrollViewer>
</Window>
