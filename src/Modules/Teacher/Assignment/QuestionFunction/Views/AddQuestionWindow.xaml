<Window
    x:Class="LaboratoryApp.src.Modules.Teacher.Assignment.QuestionFunction.Views.AddQuestionWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:domain="clr-namespace:LaboratoryApp.src.Core.Domains"
    xmlns:helper="clr-namespace:LaboratoryApp.src.Core.Helpers"
    xmlns:local="clr-namespace:LaboratoryApp.src.Modules.Teacher.Assignment.QuestionFunction.Views"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Thêm câu hỏi mới"
    Width="800"
    Height="450"
    Background="{DynamicResource MaterialDesignPaper}"
    FontFamily="{DynamicResource MaterialDesignFont}"
    Style="{StaticResource MaterialDesignWindow}"
    TextElement.FontSize="13"
    TextElement.FontWeight="Regular"
    TextElement.Foreground="{DynamicResource MaterialDesignBody}"
    TextOptions.TextFormattingMode="Ideal"
    TextOptions.TextRenderingMode="Auto"
    mc:Ignorable="d">
    <Grid Margin="10">
        <StackPanel>
            <ComboBox
                materialDesign:HintAssist.Hint="Loại câu hỏi"
                DisplayMemberPath="DisplayName"
                ItemsSource="{Binding QuestionTypeOptions}"
                SelectedValue="{Binding SelectedQuestionType, Mode=TwoWay}"
                SelectedValuePath="Value"
                Style="{StaticResource MaterialDesignOutlinedComboBox}" />

            <Button
                Margin="0,5"
                Command="{Binding AddQuestionCommand}"
                Content="Tạo câu hỏi" />
        </StackPanel>
    </Grid>
</Window>
