<Window x:Class="LaboratoryApp.src.Modules.Teacher.Assignment.ExerciseFunction.Views.AddExerciseWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LaboratoryApp.src.Modules.Teacher.Assignment.ExerciseFunction.Views"
        xmlns:domain="clr-namespace:LaboratoryApp.src.Core.Domains"
        xmlns:helper="clr-namespace:LaboratoryApp.src.Core.Helpers"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Height="685" MaxHeight="685"
        Width="400" MaxWidth="400"
        Title="Thêm bài tập mới">

    <StackPanel Margin="10">
        <!-- Thông tin chung -->
        <TextBlock Text="Thông tin chung"
           FontSize="20"
           FontWeight="Bold"
           Margin="0,0,0,5"/>

        <TextBox VerticalAlignment="Top"
                 materialDesign:HintAssist.Hint="Tên bài tập"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 Margin="0,0,0,20">
            <TextBox.Text>
                <Binding Path="Exercise.Title"
                         UpdateSourceTrigger="PropertyChanged">
                    <Binding.ValidationRules>
                        <domain:NotEmptyValidationRule ValidatesOnTargetUpdated="True"/>
                    </Binding.ValidationRules>
                </Binding>
            </TextBox.Text>
        </TextBox>

        <TextBox VerticalAlignment="Top"
                 materialDesign:HintAssist.Hint="Mô tả (tùy chọn)"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 Margin="0,0,0,20"
                 Text="{Binding Exercise.Description, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBox VerticalAlignment="Top"
                 materialDesign:HintAssist.Hint="Hướng dẫn (tùy chọn)"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 Margin="0,0,0,20"
                 Text="{Binding Exercise.Instruction, UpdateSourceTrigger=PropertyChanged}"/>
        
        <!-- Cài đặt -->
        <TextBlock Text="Cài đặt"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0,0,0,5"/>

        <PasswordBox VerticalContentAlignment="Top"
                     materialDesign:HintAssist.Hint="Mật khẩu (Tùy chọn)"
                     materialDesign:TextFieldAssist.HasClearButton="True"
                     helper:PasswordBoxHelper.Attach="True"
                     helper:PasswordBoxHelper.Password="{Binding Exercise.Password, Mode=TwoWay}"
                     Style="{StaticResource MaterialDesignOutlinedRevealPasswordBox}"
                     Margin="0,0,0,20"/>

        <ComboBox IsEditable="False"
                  materialDesign:HintAssist.Hint="Loại bài tập"
                  Style="{StaticResource MaterialDesignOutlinedComboBox}"
                  ItemsSource="{Binding DataContext.ExerciseTypeOptions, RelativeSource={RelativeSource AncestorType=Window}}"
                  ScrollViewer.VerticalScrollBarVisibility="Hidden"
                  DisplayMemberPath="DisplayName"
                  SelectedValuePath="Value"
                  SelectedValue="{Binding Exercise.Type, Mode=TwoWay}"
                  Margin="0,0,0,20"/>

        <ComboBox IsEditable="False"
                  materialDesign:HintAssist.Hint="Độ khó"
                  Style="{StaticResource MaterialDesignOutlinedComboBox}"
                  ItemsSource="{Binding DataContext.DifficultyLevelOptions, RelativeSource={RelativeSource AncestorType=Window}}"
                  ScrollViewer.VerticalScrollBarVisibility="Hidden"
                  DisplayMemberPath="DisplayName"
                  SelectedValuePath="Value"
                  SelectedValue="{Binding Exercise.Difficulty, Mode=TwoWay}"
                  Margin="0,0,0,20"/>

        <TextBox VerticalAlignment="Top"
                 materialDesign:HintAssist.Hint="Thời gian làm bài theo phút (tùy chọn)"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 Margin="0,0,0,20"
                 Text="{Binding Exercise.TimeLimitMinutes, UpdateSourceTrigger=PropertyChanged}"/>

        <StackPanel Orientation="Horizontal"
                    Margin="0,0,0,20">
            <CheckBox Content="Trộn câu hỏi"
                  IsChecked="{Binding Exercise.ShuffleQuestions}"
                  Margin="0,0,20,0"
                  Padding="5,0,0,0"/>
            <CheckBox Content="Hiển thị đáp án"
                  IsChecked="{Binding Exercise.ShowAnswersAfterCompletion}"
                  Padding="5,0,0,0"/>
        </StackPanel>

        <!-- Nút -->
        <Button Width="150"
                Content="Done"
                Background="SpringGreen"
                Foreground="Black"
                BorderThickness="0"
                Style="{StaticResource MaterialDesignRaisedSecondaryButton}"
                materialDesign:ButtonAssist.CornerRadius="10"
                Command="{Binding SaveCommand}"
                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>

    </StackPanel>
</Window>
