<UserControl x:Class="LaboratoryApp.Views.English.UC.SelectionResultUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LaboratoryApp.Views.English.UC"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="13"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             Background="{DynamicResource MaterialDesignPaper}"
             FontFamily="{DynamicResource MaterialDesignFont}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <!-- Headword and pronunciation -->
            <TextBlock Text="{Binding word}" FontSize="20" FontWeight="Bold" />
            <TextBlock Text="{Binding pronunciation}" FontStyle="Italic" Margin="0,0,0,10"/>

            <!-- POS list -->
            <ItemsControl ItemsSource="{Binding pos}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0,5,0,10">
                            <TextBlock Text="{Binding pos}" FontWeight="Bold" Foreground="DarkSlateBlue" />

                            <!-- Definition list -->
                            <ItemsControl ItemsSource="{Binding definitions}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="10,2,0,5">
                                            <TextBlock Text="{Binding definition}" TextWrapping="Wrap" />

                                            <!-- Examples -->
                                            <ItemsControl ItemsSource="{Binding examples}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <StackPanel Orientation="Horizontal">
                                                            <TextBlock Text="• " FontWeight="Bold" Foreground="Gray" />
                                                            <TextBlock Text="{Binding example}" TextWrapping="Wrap" Margin="15,0,0,2"/>
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Grid>
</UserControl>
